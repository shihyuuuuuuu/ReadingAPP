import 'package:flutter/material.dart';

class ChatNotePage extends StatelessWidget{
  final bookId;
  ChatNotePage({
    required this.bookId,
  });

  @override
  Widget build(BuildContext context) {
    
    return const Center(child: Text('chat note Page'));
  }
}