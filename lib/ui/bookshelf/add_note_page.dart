import 'package:flutter/material.dart';

class AddNotePage extends StatelessWidget{
  final bookId;
  AddNotePage({
    required this.bookId,
  });

  @override
  Widget build(BuildContext context) {
    
    return const Center(child: Text('add note Page'));
  }
}
