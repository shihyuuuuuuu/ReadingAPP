import 'package:flutter/material.dart';

class SearchBookPage extends StatelessWidget{
  // final bookId;
  @override
  Widget build(BuildContext context) {
    
    return const Center(child: Text('search book page'));
  }
}