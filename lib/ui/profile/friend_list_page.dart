import 'package:flutter/material.dart';

class FriendListPage extends StatelessWidget{
  // final bookId;
  // FriendListPage({
  //   required this.bookId,
  // });

  @override
  Widget build(BuildContext context) {
    
    return const Center(child: Text('friend list Page'));
  }
}